<?php
<#assign licenseFirst = "/* ">
<#assign licensePrefix = " * ">
<#assign licenseLast = " */">
<#include "${project.licensePath}">

<#if namespace?? && namespace?length &gt; 0>
namespace ${namespace} {
</#if>

/**
 * Description of ${name}
 *
 * @author ${user}
 * @copyright ${copy}
<#if namespace?? && namespace?length &gt; 0>
 * @package ${namespace}
</#if>
 * @created ${date} ${time}
 */
class ${name} {
    //put your code here
}
<#if namespace?? && namespace?length &gt; 0>

}
</#if>